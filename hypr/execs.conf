#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = awww-daemon && sleep 1 && awww img ~/repos/dotfiles/wallpapers/wallhaven-ayanami.png --transition-type center --transition-fps 144 --transition-bezier .7,.22,.3,.78 --transition-duration 2
exec-once = hypridle
# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
